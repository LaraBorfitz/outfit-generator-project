




export const LOGIN = (token) => ({
    type: 'LOGIN',
    payload: token,
  });
  
  // Acción para eliminar un producto del carrito
  export const GET_TOKEN = (token) => ({
    type: 'GET_TOKEN',
    payload: token,
  });